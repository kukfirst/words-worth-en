# ⚠️ The game and its script NEVER go in here. The first net is the export
# (tools/export_public.py); this is the second, in case of a manual copy.
*.hdi
*.hdm
*.d88
*.xdf
*.mes
*.rkt
game/
work/
en/
units.json
emu/text_index.json
# built from text/ by the cockpit: the whole script again
emu/text_index_pack.json
progress.jsonl
*.jsonl

# juice is fetched by a script, not vendored here: it has no licence
tools/juice/

# working clutter
__pycache__/
*.pyc
.venv*/
*.log
*.state
emu/replay/
emu/shots/
emu/rec/
emu/save/
emu/system/
# the floors this copy's cockpit has walked, and its snapshots
emu/atlas.json
emu/play_states/

# the proofreading text export -- never committed
text/
